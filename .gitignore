# Ignore actual mod files but keep metadata
mods/*.jar
optional-mods/*.jar

# Keep metadata files
!mods/*.pw.toml
!optional-mods/*.pw.toml

# Resource packs - ignore zips but keep metadata
resourcepacks/*.zip
!resourcepacks/*.toml
!resourcepacks/*.pw.toml

# Ignore cache and local files
.packwiz-cache/
*.zip
